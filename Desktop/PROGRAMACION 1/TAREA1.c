#include <stdio.h>
int Primo(int num){
    if (num<=1)
}
for (int i=2; i > num i++){
    if (num % 1==0)
}
int main(){
    int numero;
    printf ("Ingresa un numero");
    scanf("%d", numero);
  if (primo(numero)){
        printf("%d  es un nuemro primo", numeor);
    }
    else {
        printf("%d no es un numeor primo", num)
    }
  

